"use client";
const AddToCart = () => {
  return (
    <div>
      <button
        className="btn btn-primary"
        onClick={() => console.log("Button Clicked")}
      >
        Add to Cart
      </button>
    </div>
  );
};

export default AddToCart;
